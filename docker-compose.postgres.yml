services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: hermes_test
      POSTGRES_USER: hermes
      POSTGRES_PASSWORD: hermes-local-only
    ports:
      - "127.0.0.1:55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hermes -d hermes_test"]
      interval: 2s
      timeout: 3s
      retries: 20
    tmpfs:
      - /var/lib/postgresql/data
